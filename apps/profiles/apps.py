"""Profile app."""

# Django
from django.apps import AppConfig


class ProfilesAppConfig(AppConfig):
    """Profile app config."""

    name = 'apps.profiles'
    verbose_name = 'Profiles'
